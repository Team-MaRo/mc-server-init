name: Dependabot Auto-Merge

on:
  pull_request:

permissions:
  pull-requests: write
  contents: write

jobs:
  auto-merge:
    name: Auto-Merge Dependabot PRs
    runs-on: ubuntu-latest

    # Checking the actor will prevent your Action run failing on non-Dependabot PRs
    if: github.event.pull_request.user.login == 'dependabot[bot]' && github.repository == 'Team-MaRo/mc-server-init'

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Fetch Dependabot metadata 🔍
        uses: dependabot/fetch-metadata@v3
        id: metadata
        with:
          github-token: ${{ github.token }}

      - name: Approve Dependabot PR if not already approved ✅
        if: steps.metadata.outputs.update-type == 'version-update:semver-minor' || steps.metadata.outputs.update-type == 'version-update:semver-patch'
        env:
          PR_URL: ${{ github.event.pull_request.html_url }}
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          gh pr checkout "$PR_URL" # sets the upstream metadata for `gh pr status`
          if [ "$(gh pr status --json reviewDecision -q .currentBranch.reviewDecision)" != "APPROVED" ]; then
            gh pr review --approve "$PR_URL"
          else
            echo "PR already approved, skipping additional approvals to minimize emails/notification noise."
          fi

      - name: Enable auto-merge for Dependabot PR 🪄
        if: steps.metadata.outputs.update-type == 'version-update:semver-minor' || steps.metadata.outputs.update-type == 'version-update:semver-patch'
        env:
          PR_URL: ${{ github.event.pull_request.html_url }}
          # PAT (not GITHUB_TOKEN) so the merge commit fires master-push
          # workflows — release.yml's release-please run picks up the merged
          # `chore(deps)` commit. Pushes made with GITHUB_TOKEN don't trigger
          # further workflow runs.
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
        run: gh pr merge --auto --merge "$PR_URL"
